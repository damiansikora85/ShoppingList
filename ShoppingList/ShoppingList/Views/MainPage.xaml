<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:ShoppingList.Views"
             x:Class="ShoppingList.Views.MainPage">

    <ContentPage.ToolbarItems>
        <ToolbarItem x:Name="AddNewButton" Text="New Shopping" Clicked="AddNewList_Clicked" />
    </ContentPage.ToolbarItems>

    <ListView x:Name="ShoppingListView"
				ItemsSource="{Binding ShoppingLists}"
                IsPullToRefreshEnabled="true"
                VerticalOptions="FillAndExpand"
                ItemSelected="OnListSelected">

        <ListView.ItemTemplate>
            <DataTemplate>
                <ViewCell>
                    <StackLayout Orientation="Horizontal">
                        <Label Text="{Binding Name}" FontSize="16"/>
                        <Label Text="{Binding ItemsNum}" FontSize="13"/>
                        <Button Text="Del"  Command="{Binding RemoveListCommand}" HorizontalOptions="EndAndExpand"/>
                    </StackLayout>
                </ViewCell>
            </DataTemplate>
        </ListView.ItemTemplate>
    </ListView>

</ContentPage>